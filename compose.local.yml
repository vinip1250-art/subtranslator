services:
  tradutor:
    build: .
    container_name: tradutor
    environment:
      - PUBLIC_URL=http://localhost:3001
    ports:
      - "3001:3000"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3000/health"]
      interval: 30s
      timeout: 5s
      retries: 3
